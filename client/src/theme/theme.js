import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#32283c",
      light: "#fff",
    },
    secondary: {
      main: "#DC3535",
    },
    otherColor: {
      main: "#FEFCF3",
    },
  },
});
